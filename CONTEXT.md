# 错题本

考研备考用的错题整理与复习应用。把做错的题拍下来，让 VLM 打标签并可就题讨论，再用 FSRS 排复习。

## Language

### 核心实体

**错题 (Question)**:
一道被拍下来的题目，是复习的最小单位。
_Avoid_: 题目、卡片、条目、记录

**题图 (Question image)**:
错题的题干照片。每道错题必有且只有一张。
存下来的题图**就是框选并拉正之后的那一张** —— 原图在处理完就丢了（ADR-0007），所以「拉正后的图」不是另一个概念，它就是题图本身。
_Avoid_: 原图、截图、卡片图

**答案图 (Answer image)**:
错题的答案或解析照片。可选 —— 手边没有答案时也能先入库。
_Avoid_: 解析图、参考图

### 分类

**标签 (Tag)**:
挂在错题上的分类，分三层：学科 > 章节 > 知识点。
_Avoid_: 分类、类目、category

**学科 / 章节 / 知识点 (Subject / Chapter / Knowledge point)**:
标签的三个层级。顶层学科由用户自行填写，不硬编码。

### 复习

**复习 (Review)**:
一次复习动作：显示题图 → 自己评级 → 看答案图 / 与 VLM 讨论。

**评级 (Rating)**:
复习后自评的四档：Again / Hard / Good / Easy。这是 FSRS 的评分档位。
_Avoid_: 打分、评分、难度

**到期 (Due)**:
FSRS 算出的、该复习某道错题的时刻。

**复习队列 (Review queue)**:
当前已到期的错题集合。

**复习量 (Workload)**:
VLM 给出的「今天建议再做几道」。它是一道**闸**，不是调度器 —— 采纳与不采纳都**不改变**
FSRS 算出的到期时刻，改变的只是「今天还端几道题上来」。
_Avoid_: 推荐值、配额、配额上限、plan

**每日汇总 (Daily digest)**:
每天固定时刻的那一条提醒：「今天有 N 道待复习」。当天没有到期题就**不发**。
_Avoid_: 提醒、推送、日报、notification

**FSRS**:
决定「到期」的间隔重复算法。

### VLM 协作

**讨论 (Discussion)**:
与 VLM 就某道错题展开的对话，挂在错题上，可回看。

**待批准改动 (Pending change)**:
VLM 提议的、尚未生效的数据修改。批准前不影响正式数据。
_Avoid_: 暂存、草稿、draft

### 数据

**导出包 (Export bundle)**:
打包成 zip 的备份：数据库 + 图片，外加两份不含凭据的设置（复习参数与提醒钟点）。
_Avoid_: 备份文件、导出文件

**导入 (Import)**:
把一个导出包装回本机。它是**整体替换**当前那份库（旧库改名留一手），不是合并；而且只能在**下次启动**那一刻落地 —— 库一旦开着就换不动了。
_Avoid_: 恢复、还原、restore、同步
