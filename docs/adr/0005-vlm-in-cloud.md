# VLM 走云端，不走端侧

语义标签与题目讨论都由云端 VLM 提供（DeepSeek，OpenAI 兼容端点），不做端侧推理。

驱动因素：本项目要的不只是「看图打标签」，而是**能协助管理数据的 Agent 能力**，这需要强模型。端侧 VLM 在安卓上要过模型体积、内存、推理速度三关。

代价两条：题目照片离开本机；所选视觉模型带 `Exp` 后缀（实验性，官方声明可能被修订或替换）。因此接入层必须保留可替换的 provider 抽象，**不得硬编码模型 ID**。
