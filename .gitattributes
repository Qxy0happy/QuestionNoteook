# 统一行尾：仓库内一律 LF。否则 Windows 上 git 会一直刷
# "LF will be replaced by CRLF" 警告，而且 Unix shell 脚本
# （build/android/gradlew、build/linux/nfpm/*.sh）被写出 CRLF 后就废了。
* text=auto eol=lf

# Windows 批处理得是 CRLF —— cmd.exe 对纯 LF 的 .bat 不总是可靠。
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 二进制：别做行尾转换
# （签名钥匙这两条不是形式主义：上面那条 `* text=auto` 要是把 .p12 判成文本，
#   进了仓库再取出来就是另一个文件了 —— 钥匙废掉，而它换不得，见 build/android/keystore/）
*.p12 binary
*.jks binary
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.icns binary
*.ttf binary
*.jar binary
*.so binary
*.exe binary
*.apk binary
*.zip binary
