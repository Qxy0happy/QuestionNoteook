# 08: 复习：今日队列 + 四档评级 + FSRS

**What to build:** 打开复习能看到当前**已到期**的错题队列；做完一道用四档**评级**自评，应用算出下一次**到期**时间并落库。

**Blocked by:** 05（入库与题库列表）

**Status:** ready-for-human —— 服务层 9 条测试全过（含票据点名的那条）；界面只过类型检查与构建，**真机未验**

- [x] 队列只包含已到期的错题
- [x] 复习界面先只显示题图，让用户自己先做一遍
- [x] 四档评级（Again / Hard / Good / Easy）可用
- [x] 评级交给 Go 侧（官方 `go-fsrs/v4`，FSRS-6）算出下次到期，并落库
- [x] 复习记录被写下（评级、时间、当时算出的间隔）
- [x] Go 侧测试：同一道题连喂四次评级，断言 Good 比 Again 推得远
- [x] 评过的题在本次队列里不再出现
- [x] 能看到今天还剩多少道

## Comments

### 「到期」是**天**，不是**刻**

判定写成 `due < endOfToday(now)`，不是 `due <= now`。理由是这三处问的都是**天**：本票的标题
是「今日队列」、票 12 要「当天到期的数量」、票 14 要「今日到期多少」。按瞬时比会出两个毛病：
「今天还剩多少道」在一整天里自己往上涨（晚上 8 点复习过的题，第二天早上还没到期，数字反而变小）；
票 12 那条定点通知在早上会报 0 道、晚上才报真实数字，用户只会觉得它坏了。

代价（认下）：到期日是今天的题**今天一整天都在队列里**，晚上 23:00 才到期的题早上就能做。
这对复习无害 —— 提前做，评级照常，下次到期从评级那一刻往后推。

### 时区只有一处需要想

库里存的全是**绝对时刻**（Unix 毫秒 UTC），用户看的是**本地日历**。整个判定里唯一涉及时区的
地方就是「今天」，把它换算成一条绝对时刻的界线（本地日的次日零点），剩下的就是一次纯粹的比大小：

```go
func endOfToday(now time.Time) time.Time {
	y, m, d := now.Date()          // 用 now 自己的时区取日期
	return time.Date(y, m, d, 0, 0, 0, 0, now.Location()).AddDate(0, 0, 1)
}
```

用 `now.Location()` 而不是写死 `time.Local`，是为了让测试能传一个固定时区的时刻进来。
`TestQueueUsesLocalDay` 用 `UTC+8` 造了一个**本地日与 UTC 日答案相反**的时点，
所以这条不是「碰巧过了」，是真的把时区钉住了。

### `EnableShortTerm = false` 是**故意**偏离默认值的

官方 `fsrs.DefaultParam()` 带学习步，四档间隔是**分钟级**的（Again 1 分 / Hard 6 分 / Good 10 分）。
那样的话刚评 Again 的题 60 秒后刷新就会**重新出现在队列里**，与「评过的题在本次队列里不再出现」
直接冲突，而且「今天还剩多少道」会在同一分钟里自己变。

改成 long-term 调度器之后，四档里最短的一档也被推到**一天之后**（`nextInterval` 里的 `max(…, 1)`，
再被 Again ≤ Hard−1 ≤ Good−2 ≤ Easy−3 逐档拉开），于是「评过的题今天不会再出现」是**结构上的保证**，
不是靠概率。要退回默认只改这一行。权重没动（还是官方默认向量，ADR-0002 只钉算法版本与权重）。

实测（四份同样的题各连喂四次同一档）：

```
连喂四次 Again：间隔    1 天，下次到期 2026-09-20
连喂四次 Hard ：间隔   15 天，下次到期 2026-10-16
连喂四次 Good ：间隔  196 天，下次到期 2027-06-13
连喂四次 Easy ：间隔 1875 天，下次到期 2033-02-17
```

### schema（迁移 3）

`review_states` 一题一行、`WITHOUT ROWID`、主键 `question_id`（外键级联，题没了状态跟着走），
列与 `fsrs.Card` **一一对应**：`due_at` / `stability` / `difficulty` / `scheduled_days` /
`reps` / `lapses` / `state` / `last_review_at` / `remaining_steps`，外加 `(due_at)` 索引。
一一对应是为了让「读出来 → 喂 FSRS → 写回去」是一条直路，不出现「我们的字段和算法要的字段对不上、
某处需要补算」的缝。`stability`/`difficulty` 用 REAL —— 取整会把算法算出来的东西弄丢。

`review_logs` 记每次评级。**里面那个 `scheduled_days` 是「这次算出来的」**，不是复习之前的那个
（`fsrs.ReviewLog` 里带的间隔记的是复习**之前**的，顺手拿会拿错一次复习）。日志里额外汇总了当时的
`due_at`/`stability`/`difficulty` —— FSRS 的输出本来就在手上，不留下来，「最近的表现」（票 14）
以后只能反推。

**不预建零值行**：「还没复习过」这件事，没有行本身就说得清楚。预建一行的代价是每个读它的地方
都得再判一次「这行是真的还是占位的」。队列那侧用 `COALESCE(rs.due_at, q.created_at)` 表达
「新题从被拍下来那一刻起就到期」——一句话就说完了「刚拍完立刻出现在队列里」和「拖了很久的排在最前」。

### 两处越界改动（本次实现动的，已确认接受）

1. **`internal/tags/service_test.go` 改了 2 行。** `TestUpgradeFromOlderSchema` 用「删掉标签两张表 +
   `PRAGMA user_version = 1`」**扮演**票据 05 年代的老库，但它没删迁移 3 建的两张表，于是重开时
   迁移 3 撞上「表已存在」直接失败。这不是生产问题，是那个测试的退回动作**没有跟着迁移列表长大**。
   补了两行 `DROP TABLE review_logs / review_states`，并在那里留了注释说明维护要求。
   更彻底的做法是让那个测试按迁移表反推要删什么，但那要改 `internal/library` 的迁移暴露面，留待以后。
2. **`go.mod` 的 `go 1.25.0` → `go 1.26`**：`go-fsrs/v4` 自己的 go.mod 要求 1.26，`go get` 顺势抬了
   本模块的版本行。本机工具链是 go1.27.1，`build`/`vet`/`test` 全过。
   顺带：票据原先以为 `go-fsrs/v4` 「已在 go.mod 里」，其实不在，是这次 `go get` 进来的。

### FSRS 接线审计（2026-09-16，用户问「你确定是完好实现了吗」）

**做法：去看库的源码 + 在运行时断言，不信我自己写的注释。** 结论分两半。

**对的这一半（现在有测试钉住了）**：

- 库**确实是 FSRS-6**：`weights.go` 原文写着 *"Weights holds the **21 FSRS v6 weight parameters**"*，
  `DefaultWeights()` 是 v6 的默认向量。ADR-0002 那条说法站得住。
- 四档的数值与库里**逐位相同**：`Again=1 / Hard=2 / Good=3 / Easy=4`。
- `DefaultParam()` 的默认：`RequestRetention 0.9`、`MaximumInterval 36500`、`EnableFuzz false`、
  `EnableShortTerm true`。
- **`params_test.go` 把它钉死了**（三条）：
  1. `TestSchedulerDeviatesFromDefaultsInExactlyOnePlace` —— 逐字段与官方默认比，
     **只允许一处偏离**（就是学习步关掉）。它防的是一类**静默**的坏：库升级之后默认权重/保留率
     悄悄变了、间隔整体变了，而没有东西会红。要再偏离一处，得先改这条测试。
  2. `TestFourRatingsAreStrictlyOrderedAndAtLeastOneDay` —— 四档**严格递增**，
     且最短的一档也**至少一天**（那是「评过的题本次不再出现」的结构保证的落点）。
  3. `TestDueIsTheIntervalAwayFromTheReviewMoment` —— `Due` 是**复习那一刻 + N 天**的绝对时刻，
     并双向断言它与「按本地日取」是自洽的：复习**当天**不在队列里、**第 N 天**在队列里。

实测（按时复习，时钟跟着到期走）：

```
新卡：                 Again  1 天 / Hard   2 天 / Good   3 天 / Easy   8 天
复习过三次的卡：       Again  3 天 / Hard 141 天 / Good 196 天 / Easy 318 天
```

**顺带发现的两件事**（都不是 bug，是**要你决定**的）：

1. **模糊化（fuzz）是关的** —— 这是官方库的默认，我们没打开。后果：**同一批评级的题会永远落在同一天到期**
   （今天拍的一批题都评 Good，196 天后同一天全到期）。打开之后间隔会散开几个百分点，复习量就不会一阵一阵的。
2. **间隔会长到几年** —— 复习四次 Easy 是 **1875 天（5 年）**。这不是算错了（FSRS 就是这么算的），
   但考研是 6–12 个月的尺度，那等于「再也不出现」。想让它更密就把 `RequestRetention` 从 0.9 往上调
   （0.95 会明显缩短间隔）。

**库做得对的一处，值得记下来**：它会拒绝「上次复习在将来」的卡
（`fsrs: last review date … is after current time`）。也就是说**设备时钟被往回调**时，
评级会以一个诚实的错失败，而不是把状态写坏。

**我自己在这次审计里也错了一次**：那三条测试的第一版，「复习过的卡」那个用例是在**同一个时刻**
连喂三次 —— 那是**不现实**的，而且它打出来的数字会误导人：`elapsedDays=0` 时遗忘曲线在 R=1，
而 `exp((1-R)·w10) - 1` 恰好等于 0，于是稳定度的增长项为零、四档一直停在 1/2/3/4。
改成**每次把时钟推到到期那一刻**之后，数字才有意义（就是上面那张表）。

### 未验证

- **真机上的整条复习链路**（界面上评一下 → 库里的行变了）。
- `Review.svelte` 是「照既有绑定与 `Library.svelte` 的写法写成的」，接线后 `svelte-check` 0 错、
  构建通过，但**没有渲染过**。
- 反复评 Again 的题会一直钉在 1 天（long-term 把 lapse 后的 stability 封在上次的 stability），
  符合 FSRS 的设计意图，但值得知道：一道彻底记不住的题每天都回队列，不会越推越近。
