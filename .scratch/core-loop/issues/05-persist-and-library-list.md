# 05: 入库与题库列表

**What to build:** 拍完的**错题**落库；题库页把她们列出来，点开能看**题图**。

**Blocked by:** 04（手选框 → 透视拉正 → 落盘）

**Status:** ready-for-agent

- [ ] 错题落库，持有题图 hash 与创建时间
- [ ] 题库页以列表呈现错题
- [ ] 点开一道能看题图
- [ ] 新拍的题立刻出现在列表里
- [ ] 能删除一道错题
- [ ] Go 侧测试覆盖错题的增、删、查

## Comments

**2026-09-16 · Go 侧完成，前端（列表）未做**

`internal/library/` 已实现：错题增删查 + 迁移机制（`PRAGMA user_version`，加表只需往切片末尾追加）。18 个测试全过。

- `question_hash` 上**故意不加** UNIQUE —— 去重发生在文件层，两题共用一张图是合法的
- 开了 WAL，库文件旁会有 `-wal` / `-shm`。**票据 13 导出 zip 前必须先 checkpoint**，否则拷走的库可能缺最近的写入
- `DeleteQuestion` 只删库里的行、不碰图片文件，并返回被删记录以便调用方回收；引用计数式删文件属于票据 06

**已满足**：验收标准里「Go 侧测试覆盖错题的增、删、查」这一条。
**仍未做**：题库列表那半（前端）。

## Comments

**2026-09-16 · 前端完成，真机验证待做**

`frontend/src/Library.svelte`（新建）+ `App.svelte` 只改两处（import、第三个分支）。列表 / 点开看题图 / 两步确认删除 / 空状态都有了。

两处设计得好的：

- 用 `IntersectionObserver` 观察本页，**每次切到题库页就重拉一次** —— 覆盖了「新拍的题立刻出现在列表里」，而且不用去动 `Capture.svelte`，两个 agent 的文件边界因此是真的干净的
- 验证时在构建产物里逐个 grep 三个绑定函数的调用 ID（`List` / `Card` / `Delete`），证明 Vite 对无扩展名的 binding 路径解析是通的、且函数没被 tree-shake 掉

**未验证**（库里一道题都没有，也没 Wails 后端可连）：列表渲染、点开取图、删除、空状态的真实观感；以及 `CreatedAt` 线上是 RFC3339 字符串这个假设（依据是生成的 `models.ts` 声明为 `string` 且有对应转换器 —— 推理成立但没跑过）。

**跨半依赖**：本票里「新拍的题立刻出现在列表里」要真生效，前提是拍照页在确认框选后调了 `library.Add(...)` 并落库。那是票据 04 前端那半的活。
